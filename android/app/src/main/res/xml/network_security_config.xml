<?xml version="1.0" encoding="utf-8"?>
<!-- PocketCluster operates on local network with plain HTTP (no TLS). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
